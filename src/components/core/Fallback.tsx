const Fallback = () => {
    return (
        <div className="w-screen h-screen bg-background flex items-center justify-center">
            <div className="h-8 w-8 border-b border-primary rounded-[50%] animate-spin"></div>
        </div>
    );
};

export default Fallback;
